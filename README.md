# MAE4410 - Group 3

## Setup guide
Install the venv:

```bash
python3 -m venv venv
```

Boot the venv

```bash
. venv/bin/activate
```

Install requirements

```bash
pip3 install -r requirements.txt
```